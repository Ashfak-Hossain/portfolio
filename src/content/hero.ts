import type { Hero } from '../types/content';

export const hero: Hero = {
  lede: "I build software the way I solve problems: from the invariants out. Correctness first, cleverness second — because a system you can't reason about is a system waiting to surprise you.",
  ledeEmphasis: 'from the invariants out',
};
